# Letters

Dialogue with collaborators. **Lives on the `letters` branch only — do not merge to `main`.**

Each subfolder is dated `YYYY-MM-DD_<topic>` and contains the incoming note (transcribed) plus our response/commentary.

## Index

- `2026-04-29_gunnar_messy_diagrammatics/` — Gunnar's follow-up to the Gribov draft. Two charges: (1) $\varphi^4$ exponents are counting, not asymptotics; (2) his $2^n$ DP ladder. Response shows AC reproduces both: Wick weights as $[g^k]\partial_J^2\log\mathcal{Z}$, and the ladder as a $2{\times}2$ transfer matrix with eigenvalues $\{0,2\}$.
